import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Hello, World!")
            .minimumScaleFactor(0.5) // Set the minimum scale factor to limit downscaling
    }
}
